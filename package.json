{
  "name": "dr-shaimaa-mern-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm ci --prefix server && npm ci --prefix client",
    "dev": "concurrently -k -n API,CLIENT -c blue,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "wait-on http-get://127.0.0.1:5000/api/health && npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server",
    "seed:admin": "npm run seed:admin --prefix server",
    "audit:all": "npm audit --prefix server && npm audit --prefix client"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "wait-on": "^9.1.0"
  }
}
